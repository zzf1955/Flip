- 现在的 pipeline 大概是；

G1 第一人称视频 + G1 姿态数据（关节编码器）

✓ Step 1  Pose 获取：精确的本体感知，无需视觉 pose estimation

✓ Step 2  Robot 分割+去除：FK → Mesh 投影 → SAM2 精准mask → 背景 inpaint （误差）

Step 3  运动学映射：Robot joint state → Human pose（关节方向软约束 + 关键点 IK 微调）

Step 4  Human 渲染：模型 → 深度图 → control net 重绘 （模型提供轮廓、透视，生成模型提供细节）

Step 5  得到 (合成 human, 真实 robot) 配对 → 下游视频生成，WAN2.1，LoRA

（可选）结合 World Action Model 范式，联合预测 action + video，提高视频生成的精准性

- 现在有的实验大概是：
    - 相机标定实验
        - 4 point 校对
        - 5 point 校对
        - mask 单帧校对
        - 模式：相机模型/关节自由度
    - inpaint pipeline
        - 数据获取
            - video 定位
            - State 定位
        - Mesh 渲染
            - 视频 overlay
        - mask 生成
        - box 生成
        - SAM2 分割
        - ProPointer 背景重绘
    - human mesh 姿态校对
        - 单帧九宫格校对
        - 视频 overlay

1. 还有哪些脚本？
2. 当前的代码结构是什么样的？是不是很乱？

- introduction
    - background
        - world model 范式、视频的重要性
        - 机器人控制从纯机械臂转向人形机器人
    - challenge
        - sim2real gap：传统方式依赖渲染产生结构精确的机器人视频，但是存在光照、材质等问题
        - 视频中机器人运动的准确性：视频生成模型难以保证运动姿态的准确性
        - 第一视角人形难处理
            - 画面边缘 OOD：第一视角下肢体多分布在画面边缘，传统文生图模型/文生视频模型难以处理。并且画面视角不完全，通常无法看到全身。
            - （备选，只是更复杂，可以不写）人形形态更复杂：人形机器人的结构和人类结构相差较大，难以对齐；人形机器人关节更复杂，需要模型更强的表达能力。


现在需要推进下一步

1. 手指动作映射调整
2. IK 优化
3. 深度图提取 + 人体重绘

现在需要推进下一步
人体渲染基本跑通了，现在需要在视频中做：
1. 深度图提取：把有人体的视频整体做深度图的提取
2. 对应部分深度图模糊，因为人体的并不准
3. 提供重绘mask
4. 按照深度图 + mask，重新生成视频

把当前人体模型换成真人，白色上衣，黑色裤子，拖鞋。人物与环境光影一致。
把当前机器人换成真人，白色上衣，黑色裤子，拖鞋。人物与环境光影一致。