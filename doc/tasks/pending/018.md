---
id: "018"
title: "文档整理：CLAUDE.md 精简 + 训练 infra 独立 + 模块化索引"
status: "pending"
branch: ""
worktree: ""
review: ""
review_at: ""
created_at: "2026-04-21T12:00:00Z"
updated_at: "2026-04-21T12:00:00Z"
---

## 背景

CLAUDE.md 439 行，后半段（L297-439）全是训练命令和 DDP 细节，与项目概述不在一个层级，阅读负担重。文档索引是平铺列表，缺少模块分类。`step_5_wan22_ti2v5b.md` 和 `step_5_wandb_setup.md` 未出现在索引中。

## 目标

1. CLAUDE.md 从 439 行降到 ~310 行，聚焦项目概述和规范
2. 训练基础设施内容独立为 `doc/step_5_training_infra.md`
3. 文档索引按 5 个模块分类：数据 / Human 渲染 / Seedance API / 微调算法 / 项目管理
4. 补充开发规范提示（走 `/develop` skill + 及时更新文档）

## 范围

**包含：**
- 新建 `doc/step_5_training_infra.md`（从 CLAUDE.md 迁移训练内容）
- 编辑 CLAUDE.md（删除训练 section、重组文档索引、加开发规范提示）

**不包含：**
- 不修改任何现有 `doc/step_*.md` 文件内容
- 不更新 `scripts_inventory.md` 代码架构（如需要另开 task）
- 不新增视频 Inpaint 模块文档（当前无独立文档，不强建）

## 实施计划（分步骤）

### Step 1: 新建 `doc/step_5_training_infra.md`

从 CLAUDE.md L297-439 迁移以下内容：
- "DDP 训练注意事项" section（模型加载 / GPU 直接读取）
- "LoRA 微调流程" section（Wan 2.1 data_process + 训练、Wan 2.2 两阶段策略命令、统一训练入口、Legacy 命令、训练目录结构）

加顶部说明交代文档定位。

### Step 2: 编辑 CLAUDE.md

**2a.** 删除 L297-439（"## DDP 训练注意事项" + "## LoRA 微调流程"），替换为一行引导：
```markdown
## 训练

详见 [`doc/step_5_training_infra.md`](doc/step_5_training_infra.md)（DDP / GPU 直接加载 / 数据 cache / 训练命令 / 目录结构）。
```

**2b.** 重组 "## 文档索引"（L249-266），按模块分类：数据 / Human 渲染 / Seedance API / 微调算法 / 项目管理。补上之前遗漏的 `step_5_wan22_ti2v5b.md` 和 `step_5_wandb_setup.md`。

**2c.** 在顶部 "如果用户直接提出了需求，请使用 flip/.claude/skills/develop/SKILL.md" 之后追加：
```
开发完成后及时更新对应模块的文档。
```

### Step 3: 验证

- CLAUDE.md 行数 ~310（删 ~140，加 ~10）
- `doc/step_5_training_infra.md` 可独立阅读，训练命令完整
- 文档索引覆盖所有 14 个 doc/*.md 文件
- 无内容丢失

## 验收标准

1. CLAUDE.md 不再包含训练命令和 DDP 细节，仅有一行引导链接
2. `doc/step_5_training_infra.md` 存在且内容与原 CLAUDE.md 一致
3. 文档索引按模块分类，覆盖全部 doc/*.md 文件
4. 顶部有开发规范提示

## 测试要求

纯文档变更，无代码测试。验证方式：
- 人工检查 CLAUDE.md 和新文档内容完整性
- 确认文档索引中每个链接指向的文件存在
